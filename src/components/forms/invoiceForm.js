import { useState, useEffect } from "react"
const AquaInvoiceForm = (props) => {
    const { title, inputvalues, handleChange, handleSubmit, } = props
    return (
        <>
            <h4>{title}</h4>

        </>
    )
}
export default AquaInvoiceForm